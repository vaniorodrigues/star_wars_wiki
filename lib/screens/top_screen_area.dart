import 'package:flutter/material.dart';

class TopScreenArea extends StatelessWidget {
  const TopScreenArea({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
